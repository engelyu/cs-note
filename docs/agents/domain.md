# Domain docs

This is a single-context repository. Before exploring code, read the root `CONTEXT.md` and the relevant records in `docs/adr/`. Use the vocabulary defined in `CONTEXT.md` when writing issue titles, specs, tests, and implementation notes. If a proposed change conflicts with an ADR, call out the conflict explicitly before proceeding.
