# CI generates static semantic artifacts

Curated examples are executed and verified in local authoring or CI workflows, then published as compact Semantic Artifacts. The Student Runtime does not compile or debug arbitrary C++ at page runtime; this preserves the site's static deployment model, keeps playback deterministic, and avoids giving every student session a compiler/debugger process.

## Status

Accepted
